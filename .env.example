# ═══════════════════════════════════════════════════════════════════════════════
# APEX OS — Environment Configuration
# v6.4.1 Wire Architecture (73 variables across all subsystems)
# ═══════════════════════════════════════════════════════════════════════════════
# LKGC RULE: Do NOT change values that are live in production.
# Copy this file to .env and fill in your values.
# ═══════════════════════════════════════════════════════════════════════════════

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 1: SUPABASE (Database + Auth)                                      │
# └─────────────────────────────────────────────────────────────────────────────┘
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
# Legacy aliases (some modules use these)
# SUPABASE_SERVICE_KEY=your_supabase_service_key
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Vibe Marketing Supabase (separate project for content/newsletter)
# VIBE_MARKETING_SUPABASE_URL=https://your-marketing-project.supabase.co
# VIBE_MARKETING_SUPABASE_SERVICE_KEY=your_marketing_service_key

# Direct DB access (migrations only)
# DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres
# SUPABASE_DB_PASSWORD=your_db_password

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 2: EMAIL (Resend — Waitlist + Notifications)                       │
# └─────────────────────────────────────────────────────────────────────────────┘
RESEND_API_KEY=re_your_resend_api_key

# CRITICAL: Set FROM_EMAIL_VERIFIED=true ONLY after verifying your domain in Resend.
# Without this, emails go through onboarding@resend.dev (sandbox — admin only).
FROM_EMAIL_VERIFIED=false
FROM_EMAIL=APEX OS <apex@infoacademy.uk>
NOTIFY_EMAIL=fratilanico@gmail.com

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 3: NOTIFICATIONS (Telegram + Discord)                              │
# └─────────────────────────────────────────────────────────────────────────────┘
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token
# TELEGRAM_CHAT_ID=your_telegram_chat_id
# TELEGRAM_WEBHOOK_SECRET=your_telegram_webhook_secret

# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook
# DISCORD_BOT_TOKEN=your_discord_bot_token

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 4: NEWSLETTER (Listmonk — Self-hosted alternative to Resend)       │
# └─────────────────────────────────────────────────────────────────────────────┘
# LISTMONK_API_URL=https://your-listmonk-instance.com/api
# LISTMONK_USERNAME=admin
# LISTMONK_PASSWORD=your_listmonk_password

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 5: AI PROVIDERS (Multi-Tier Fallback)                              │
# └─────────────────────────────────────────────────────────────────────────────┘
# Tier 1: Perplexity (coding, web-grounded) — https://perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_perplexity_api_key
# PERPLEXITY_MODEL=sonar-reasoning-pro

# Tier 2: Groq (fast, free tier) — https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key
# GROQ_MODEL=llama-3.1-70b-versatile

# Tier 3: Google Gemini (free tier) — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key
# GEMINI_MODEL=gemini-1.5-flash

# Tier 4: Cohere (free tier) — https://dashboard.cohere.com/api-keys
COHERE_API_KEY=your_cohere_api_key
# COHERE_MODEL=command-r

# Tier 5: Kimi (optional) — https://kimi.ai
# KIMI_API_KEY=your_kimi_api_key

# OpenAI (for embeddings in knowledge base)
# OPENAI_API_KEY=your_openai_api_key

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 6: GOOGLE CLOUD / VERTEX AI                                        │
# └─────────────────────────────────────────────────────────────────────────────┘
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_REGION=us-central1
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GOOGLE_APPLICATION_CREDENTIALS_BASE64=base64_encoded_credentials

# Vertex AI model config
# VERTEX_PROJECT_ID=your-gcp-project-id
# VERTEX_LOCATION=us-central1
# VERTEX_MODEL=gemini-1.5-flash
# VERTEX_MODEL_FAST=gemini-1.5-flash
# VERTEX_MODEL_PRO=gemini-1.5-pro
# VERTEX_API_KEY=your_vertex_api_key
# USE_VERTEX_AI=true

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 7: KNOWLEDGE BASE (Notion + GitHub parsers)                        │
# └─────────────────────────────────────────────────────────────────────────────┘
# NOTION_TOKEN=your_notion_integration_token
# NOTION_WAITLIST_DB_ID=your_notion_database_id_for_waitlist_crm
# GITHUB_TOKEN=your_github_personal_access_token

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 8: VERCEL + DEPLOYMENT                                             │
# └─────────────────────────────────────────────────────────────────────────────┘
# VERCEL_OIDC_TOKEN=your_vercel_oidc_token
# VERCEL_URL is auto-set by Vercel at deploy time
# NEXT_PUBLIC_URL=https://infoacademy.uk

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 9: WEBSOCKET + REAL-TIME                                           │
# └─────────────────────────────────────────────────────────────────────────────┘
# VITE_WS_URL=wss://your-websocket-server.com
# APP_URL=https://infoacademy.uk
# JWT_SECRET=your_jwt_secret_for_ws_auth

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 10: CLAWBOT (Terminal Bot)                                         │
# └─────────────────────────────────────────────────────────────────────────────┘
# VITE_CLAWBOT_WS_URL=wss://your-clawbot-server.com
# VITE_CLAWBOT_TOKEN=your_clawbot_token

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 11: VOICE + MEDIA (ElevenLabs, Picovoice)                         │
# └─────────────────────────────────────────────────────────────────────────────┘
# VITE_ELEVENLABS_API_KEY=your_elevenlabs_api_key
# VITE_ELEVENLABS_VOICE_ID=your_elevenlabs_voice_id
# VITE_PICOVOICE_ACCESS_KEY=your_picovoice_access_key

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECTION 12: APP CONFIG                                                     │
# └─────────────────────────────────────────────────────────────────────────────┘
NODE_ENV=development
# ADMIN_DASH_PASSWORD=your_admin_password
# INTERNAL_API_BASE=http://localhost:3000/api

# Preview mode credentials (Vite client-side)
# VITE_PREVIEW_USER=preview
# VITE_PREVIEW_PASS=your_preview_password
